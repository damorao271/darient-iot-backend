# Server
PORT=
NODE_ENV=
# PostgreSQL (used when connecting to DB in dev)
# API Key for endpoint authentication (x-api-key or Authorization: Bearer <key>)
API_KEY=
# Identity returned by GET /auth/me (optional — defaults shown below)
AUTH_USER_ID=
AUTH_USER_EMAIL=
AUTH_USER_ROLE=
CORS_ORIGIN=

DB_PORT=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
MQTT_BROKER_URL=
