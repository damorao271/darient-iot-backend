import type { PrismaClient } from '../../generated/prisma/client.js';

export async function seedReservations(_prisma: PrismaClient): Promise<void> {
  // TODO: add reservations seeding when needed
}
