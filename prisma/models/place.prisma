model Place {
  id        String   @id @default(cuid())
  name      String
  latitude  Float
  longitude Float
  spaces    Space[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
